{
  "name": "Type-X",
  "version": "1.0.0",
  "description": "Type-X browser extension",
  "main": "index.js",
  "repository": "git@github.com:kabisa/recursive-extension.git",
  "author": "Roel Nieskens <roel@pixelambacht.nl>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -rf dist/* && cp -a src/* dist && browserify -e src/popup.js -o dist/popup.js"
  },
  "dependencies": {
    "blob-to-buffer": "^1.2.8",
    "fontkit": "^1.8.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "8",
    "browserify": "^16.5.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
